name: deepseek_balance
description: "DeepSeek Balance Desktop Tool"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  window_manager: ^0.4.2
  tray_manager: ^0.5.2
  http: ^1.2.0
  shared_preferences: ^2.3.0
  win32_registry: ^1.1.0
  path_provider: ^2.1.0
  image: ^4.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
